<hr>
<table class="table">
  <thead>
    <tr class="text-center">
      <th scope="col">No</th>
      <th scope="col">Amount</th>
      <th scope="col">Sender</th>
      <th scope="col">Receiver</th>
      <th scope="col" colspan="6">Action</th>
    </tr>
  </thead>
  <tbody>
  <% @withdraws.each_with_index do |withdraw, index| %>
    <tr class="text-center">
      <th scope="row"><%= index + 1 %></th>
      <td><%= format_rupiah(withdraw.amount) %></td>
      <td><%= withdraw.stock.code %></td>
      <td><%= "#{withdraw.account.number} - #{withdraw.account.customer.full_name}" %></td>
      <td colspan="6"><%= link_to "Show", withdraw, class: 'btn btn-secondary btn-sm' %><td>
    </tr>
  <% end %>
  </tbody>
</table>
